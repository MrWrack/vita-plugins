# v0.22 link fix

- Added `png` and `z` after `vita2d` in `target_link_libraries`.
- Fixes linker errors from `vita2d_image_png.o` (`png_create_read_struct`, `png_read_image`, etc.).
- Kept the v0.18 indexed LiveArea PNG guard intact.
- Cleaned the two misleading-indentation warnings in the progress code.
