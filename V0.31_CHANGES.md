# v0.31

- MEM HUD now uses `sceKernelGetFreeMemorySize()` and shows free user RAM in MB.
- Added `SceSysmem_stub` and `<psp2/kernel/sysmem.h>`.
- TEMP keeps verified `scePowerGetBatteryTemp()` reading.
- Passive HUD behavior retained: R+Up toggles; HUD has no selection/navigation.
- Background module documentation now explicitly separates the working hotkey state from the still-unimplemented global renderer.
- No fake global HUD claim: app-local HUD remains app-local until a Shell/kernel renderer is built and hardware-tested.
