# Vita AutoPlugin v0.35 compile fix

- Removed the unsupported/unverified userland `SceKernelFreeMemorySizeInfo` / `sceKernelGetFreeMemorySize()` path from the background HUD. MEM now reports `N-A` instead of inventing a value.
- Added `SceProcessmgr_stub` for `sceKernelGetProcessTimeWide()`.
- Silenced the module entry-point unused-parameter warnings without changing the ABI.
- Fixed GitHub Actions YAML indentation for the background-plugin step.
- Updated artifact/VPK labels to v0.35.

Global HUD remains a hardware-test prototype until the generated SUPRX is tested on a real PS Vita.
