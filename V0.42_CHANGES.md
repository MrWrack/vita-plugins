# v0.42 Kernel HUD build fix

- Removed the `memset()` dependency from the kernel input thread, avoiding the undefined libc symbol under `-nostdlib`.
- Set the ELF linker entry point explicitly to `module_start`, removing the missing `_start` linker warning without using an incompatible alias.
- Keeps the v0.41 kernel SceDisplay hook and kernel-to-user framebuffer copy approach.
