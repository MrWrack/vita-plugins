# Vita AutoPlugin – MrWrack v0.34

Build-fix release based directly on the uploaded v0.33 tree.

- Fixed `.github/workflows/build.yml` indentation for the background-plugin step.
- Removed the incompatible `_start` alias from `background_plugin/src/main.c`; `exports.yml` already declares `module_start`/`module_stop`.
- Updated artifact/version labels to v0.34.
- Kept the existing LiveArea assets and main VPK source unchanged.

The global HUD remains hardware-test status until the resulting SUPRX is tested on a PS Vita.
