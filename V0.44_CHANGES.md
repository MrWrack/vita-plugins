# v0.44 — kernel HUD MEM compile fix

- Keeps the proven v0.42 kernel framebuffer hook and v0.43 HUD layout/GPU work.
- Fixes the v0.43 MEM compile failure by defining the four-field address-space structure locally, matching the layout used by PSVshell (`base`, `total`, `free`, `unkC`).
- Uses that local type consistently for the resolved SceSysmem function pointer and stack value.
- GPU remains a live resolved value; no hard-coded MHz.
- MEM row is omitted if the metric cannot be read; no `N-A` text.
- HUD remains shifted to the right.

This source package has been ZIP-integrity checked here. It has not been VitaSDK-compiled or hardware-tested in this environment.
