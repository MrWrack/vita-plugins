# v0.30 compile fix

- Added a forward declaration for `update_fps_counter()` before its first call in `progress_frame()`.
- Fixes the implicit declaration / static declaration compiler error seen in GitHub Actions.
- Bumped UI and GitHub Actions artifact version to v0.30.
