# v0.39 - HUD outside app

- HUD work only; overclock intentionally unchanged.
- Background plugin logs startup/hook result to `ux0:data/VitaAutoPlugin/hud_plugin.log`.
- Tries the established `sceDisplaySetFrameBuf` import hook first.
- If SceShell has no matching main-module import, falls back to a `SceDisplay` export hook.
- HUD starts visible for testing; R + D-pad Up toggles it.
- MEM remains N-A until a verified memory API is selected.
